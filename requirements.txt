# Core web framework
fastapi==0.136.0
uvicorn[standard]==0.44.0
pydantic==2.13.2

# Image processing
# opencv-contrib-python is a strict superset of opencv-python and opencv-python-headless;
# mediapipe pulls in the contrib variant and pyiqa pulls in the headless one. Pinning
# contrib here gives one consistent install (`import cv2` works everywhere).
Pillow==12.2.0
# HEIF/HIF decoder — registers a Pillow plugin so PIL.Image.open() handles
# .heic/.hif. Used to render Fuji X-series camera-baked HIF previews when the
# Settings toggle "Prefer camera JPEG/HIF preview" is on.
pillow-heif==1.3.0
opencv-contrib-python==4.13.0.92
rawpy==0.26.1
numpy==2.4.4

# Phase 2 — face detection (model downloaded on first use to data/models/)
mediapipe==0.10.33

# Phase 2 module 2 — perceptual IQA (TOPIQ model ~100 MB, downloaded to ~/.cache/pyiqa/ on first use)
pyiqa==0.1.15

# Phase 2 module 3 — aesthetic appeal (LAION Aesthetic Predictor V2 linearMSE head,
# loaded directly via huggingface_hub from camenduru/improved-aesthetic-predictor;
# CLIP ViT-L/14 backbone ~890 MB downloaded to ~/.cache/huggingface/ on first use).
# Previously used simple-aesthetics-predictor — dropped after the upstream
# shunk031 HF account was suspended in 2025; we now load the same underlying
# weights with transformers + huggingface_hub directly (both already required).

# Phase 2 module 4 — SigLIP semantic similarity (base model ~300 MB, downloaded to ~/.cache/huggingface/ on first use)
# transformers also serves the CLIP backbone for module 3.
transformers>=4.40.0

# Phase 2 module 5 — face-identity embeddings for People-mode clustering.
# InceptionResnetV1 (vggface2-pretrained) ~107 MB, downloaded to ~/.cache/torch/ on first use.
#
# NOT pinned in this file: facenet-pytorch's setup.py pins an old Pillow that
# fails to build on Python 3.13, and listing it here makes pip's resolver
# refuse the whole requirements file. Runtime needs only torch / numpy /
# Pillow / requests, all already pulled in by entries above. Install once
# manually after the rest of requirements.txt:
#
#   pip install --no-deps facenet-pytorch
#
# start.sh handles this — see the post-requirements install step there.

# Metadata extraction
# ExifRead is fast and pure-Python but cannot parse RAF (Fuji). For RAF and
# any other format ExifRead can't read, exif_parser falls back to pyexiftool
# which shells out to /opt/homebrew/bin/exiftool. The exiftool binary is a
# system dependency (brew install exiftool); pyexiftool just wraps it.
ExifRead==3.5.1
PyExifTool==0.5.6

# File monitoring
watchdog==6.0.0

# Phase 3 — personal style learning (decision → delta score model)
scikit-learn>=1.4.0

# LM Studio narrative explanation layer (OpenAI-compatible local API client)
openai>=1.0.0

# Trash utility — send files to macOS Trash (recoverable), used by POST /trash-rejects
send2trash>=1.8.0

# Launcher — menu bar / tray icon
pystray>=0.19.0   # Windows / Linux tray icon
rumps>=0.4.0      # macOS-native menu bar app (ignored on other platforms)

# Environment
python-dotenv==1.2.2
